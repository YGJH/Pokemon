# 牌組過濾器選項說明

牌組過濾器目前丟棄約 53% 的對局組合。以下是五個選項：

---

### 選項 1：維持不變（推薦）

**規則**：自身牌組原型 ∈ 𝒟_self 且對手牌組原型 ∈ 𝒟_opp

目前行為。𝒟_self 外的資料訓練的是你永遠不會玩的牌組，𝒟_opp 外的資料信念頭沒有對應類別可分類。約 51,000 對局組合，約為移除專家過濾後語料庫的 6 倍（相對於之前過濾專家後的語料庫則更大）。

**優點**：乾淨，每個信念標籤都有意義，分片大小可控。
**缺點**：丟棄近半資料。

---

### 選項 2：放寬 𝒟_self / 𝒟_opp

**規則**：提高 `n_self` 和 `n_opp`，讓更多原型納入範圍

兩個列表都是僅追加的，因此這只會增加 ID 並加寬信念頭 — 不會重新編號任何東西。更多牌組可訓練，但分片會更大。

**優點**：漸進式，不破壞現有 checkpoint，可根據需要調整數量。
**缺點**：信念頭變寬 = 更多參數、更多類別需要足夠資料支撐；長尾原型的訓練樣本可能稀疏。

---

### 選項 3：只丟棄 𝒟_opp 那一半

**規則**：保留自身牌組過濾，接納任何對手

收回 53% 中的大部分，加入困難對局組合。但不在列表上的對手會拿到信念標籤 -1，對信念頭毫無貢獻。

**優點**：幾乎收回所有被丟棄的資料，自身牌組過濾仍然有意義。
**缺點**：信念頭只對少量對手原型有訊號，其餘全是 -1（無效類別）；分片體積接近全量。

---

### 選項 4：丟棄兩個牌組過濾器

**規則**：真正不過濾 — 每個有效對局、每位玩家、每副牌組都納入

約 104,000 對局組合，約 140 GB 的分片。但大多數資料訓練的是沒有任何專精模型會使用的牌組。

**優點**：零浪費，資料量最大。
**缺點**：140 GB 分片，大部分資料對給定專精模型無用（模型只玩一副牌組）；信念頭需要覆蓋數十甚至上百個對手原型，多數類別樣本稀疏；訓練時 `--archetype-self N` 仍只挑一個牌組，所以大量資料在訓練特定專精時根本不會被讀取。

---

### 選項 5：自訂

手動輸入想要的過濾邏輯，例如自訂 `n_self`/`n_opp` 數量、自訂閾值、或完全不同的過濾條件。
